# handlebars-page-viewer


## Requirements

- Nodejs & NPM


## Installation

- execute `npm install`

## Add page

- add route in `routes.js`
- add view in `view/`
- add json data file in `data/`

## Preview page

- start server with `npm start`
